import React from 'react';
import { CapsuleButton } from '@/components/ui/Button';

export default function GeneratedComponent() {
  return (
    <div
      data-component-id="dialog_btn_cancel"
      style={{
        position: 'absolute',
        left: 60,
        top: 600,
        width: 100,
        height: 40,
      }}
    >
      <CapsuleButton
        size="large"
        variant="secondary"
        onClick={() => {}}
        className="w-full"
      >
        取消
      </CapsuleButton>
    </div>
  );
}