import React from 'react';
import { CapsuleButton } from '@/components/ui/Button';

export default function GeneratedComponent() {
  return (
    <div
      data-component-id="sheet_goto_btn"
      style={{
        position: 'absolute',
        left: 16,
        top: 850,
        width: 328,
        height: 48,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <CapsuleButton
        size="large"
        variant="primary"
        className="w-full"
        onClick={() => {}}
      >
        去配单
      </CapsuleButton>
    </div>
  );
}