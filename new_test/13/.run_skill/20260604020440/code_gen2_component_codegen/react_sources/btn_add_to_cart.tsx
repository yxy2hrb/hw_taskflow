import React from 'react';
import { CapsuleButton } from '@/components/ui/Button';

export default function GeneratedComponent() {
  return (
    <div
      data-component-id="btn_add_to_cart"
      style={{
        position: 'absolute',
        left: 132,
        top: 1116,
        width: 100,
        height: 40,
      }}
    >
      <CapsuleButton variant="secondary" size="large" className="w-full h-full tf-cg-btn-cart">
        <span className="font-headline-xs" style={{ fontSize: 'var(--font-headline-14)', fontWeight: 'var(--font-weight-medium)', color: 'var(--color-text-primary)' }}>
          加入采购单
        </span>
      </CapsuleButton>
    </div>
  );
}