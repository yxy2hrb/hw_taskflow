import React from 'react';
import { CapsuleButton } from '@/components/ui/Button';

export default function GeneratedComponent() {
  return (
    <div
      data-component-id="btn_go_to_quote"
      style={{
        position: 'absolute',
        left: 200,
        top: 680,
        width: 140,
        height: 44,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <CapsuleButton
        size="large"
        variant="primary"
        onClick={() => {}}
        className="w-full"
      >
        去配单
      </CapsuleButton>
    </div>
  );
}