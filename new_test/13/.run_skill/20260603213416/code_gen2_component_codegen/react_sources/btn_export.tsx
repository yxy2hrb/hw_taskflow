import React from 'react';
import { CapsuleButton } from '@/components/ui/Button';

export default function GeneratedComponent() {
  return (
    <div
      data-component-id="btn_export"
      style={{
        position: 'absolute',
        left: 240,
        top: 880,
        width: 104,
        height: 40,
      }}
    >
      <CapsuleButton
        variant="primary"
        size="large"
        className="w-full"
        onClick={() => {}}
      >
        导出
      </CapsuleButton>
    </div>
  );
}