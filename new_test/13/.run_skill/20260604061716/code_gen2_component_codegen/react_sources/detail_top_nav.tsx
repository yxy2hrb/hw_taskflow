import React from 'react';
import TopNav from '@/components/TopNav';

export default function GeneratedComponent() {
  return (
    <div data-component-id="detail_top_nav">
      <TopNav
        variant="title"
        title=""
        onBack={() => {}}
        actions={['cart', 'message', 'grid']}
      />
    </div>
  );
}