import React from 'react';
import TopNav from '@/components/TopNav';

export default function GeneratedComponent() {
  return (
    <div data-component-id="plan_detail_top_nav">
      <TopNav
        variant="title"
        title="配单详情"
        onBack={() => {}}
        actions={['grid']}
      />
    </div>
  );
}